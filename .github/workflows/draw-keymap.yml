name: Draw Keymap
on:
  workflow_dispatch: # permite rodar manualmente
  push:
    paths:
      - "config/*.keymap" # roda sempre que o keymap mudar
      - "keymap_drawer.config.yaml"

jobs:
  draw:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Install and Draw
        uses: caksoylar/keymap-drawer@v0.17.0 # Use uma versão estável em vez de @main
        with:
          keymap_lines: 16
          output_path: "img/keymap.svg"
          # Certifique-se de que o caminho para o seu keymap está correto:
          keymap_path: "config/sofle.keymap"
      - name: Commit and Push
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add img/keymap.svg
          git commit -m "Update keymap SVG" || echo "No changes to commit"
          git push
